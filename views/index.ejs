<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="header">
      <h1><%= title %></h1>
    </div>
    <div class="main">
      <div class="message">
        <div class="link">
      <a href="/new">Create new Message</a>
    </div>
      <% for (i = 0; i < messages.length; i++) { %> 
        <h1 class="messageUser"><%= messages[i].user %> </h1>
      <div class="messageText"><%= messages[i].text %></div>
      <div class="messageDate"><%= messages[i].added.toLocaleDateString('en-US') %></div>
      <% } %> 
    </div>
  </div>
  </body>
</html>
